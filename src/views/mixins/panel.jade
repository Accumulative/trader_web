//- Panel mixin
mixin panel(heading, style)
    - var style = (typeof style === 'undefined') ? "panel-default" : style
    div( class=["panel", style] )
        .panel-heading= heading
        .panel-body
            block